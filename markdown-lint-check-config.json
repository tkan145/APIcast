{
    "ignorePatterns": [
        {
            "pattern": "^.*.foo"
        },
        {
            "pattern": "^.*.bar"
        }
    ],
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "{{BASEURL}}/"
        }
    ]
}
